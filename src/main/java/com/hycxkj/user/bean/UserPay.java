package com.hycxkj.user.bean;

import java.io.Serializable;
import java.util.Date;

import org.springframework.format.annotation.DateTimeFormat;
import com.alibaba.fastjson.annotation.JSONField;
import io.swagger.annotations.ApiModel;
import io.swagger.annotations.ApiModelProperty;
import com.hycxkj.common.annotation.PrimaryKey;
/**
 * 实体类对应的数据表为：  user_pay
 * @author 陈少平
 * @date 2018-03-29 16:51:12
 */
@ApiModel(value ="UserPay")
public class UserPay implements Serializable {
    @PrimaryKey
    @ApiModelProperty(value = "")
    private Long userPayId;

    @ApiModelProperty(value = "")
    private Long uId;

    @ApiModelProperty(value = "")
    private Long pay;

    @ApiModelProperty(value = "")
    @DateTimeFormat(pattern = "yyyy-MM-dd HH:mm:ss")
    private Date payTime;

    /**
     * This field was generated by MyBatis Generator.
     * This field corresponds to the database table user_pay
     */
    private static final long serialVersionUID = 1L;

    public Long getUserPayId() {
        return userPayId;
    }

    public void setUserPayId(Long userPayId) {
        this.userPayId = userPayId;
    }

    public Long getuId() {
        return uId;
    }

    public void setuId(Long uId) {
        this.uId = uId;
    }

    public Long getPay() {
        return pay;
    }

    public void setPay(Long pay) {
        this.pay = pay;
    }

    @JSONField(format = "yyyy-MM-dd HH:mm:ss")
    public Date getPayTime() {
        return payTime;
    }

    public void setPayTime(Date payTime) {
        this.payTime = payTime;
    }

    @Override
    public String toString() {
        StringBuilder sb = new StringBuilder();
        sb.append(getClass().getSimpleName());
        sb.append(" [");
        sb.append("Hash = ").append(hashCode());
        sb.append(", userPayId=").append(userPayId);
        sb.append(", uId=").append(uId);
        sb.append(", pay=").append(pay);
        sb.append(", payTime=").append(payTime);
        sb.append("]");
        return sb.toString();
    }

    @Override
    public boolean equals(Object that) {
        if (this == that) {
            return true;
        }
        if (that == null) {
            return false;
        }
        if (getClass() != that.getClass()) {
            return false;
        }
        UserPay other = (UserPay) that;
        return (this.getUserPayId() == null ? other.getUserPayId() == null : this.getUserPayId().equals(other.getUserPayId()))
            && (this.getuId() == null ? other.getuId() == null : this.getuId().equals(other.getuId()))
            && (this.getPay() == null ? other.getPay() == null : this.getPay().equals(other.getPay()))
            && (this.getPayTime() == null ? other.getPayTime() == null : this.getPayTime().equals(other.getPayTime()));
    }

    @Override
    public int hashCode() {
        final int prime = 31;
        int result = 1;
        result = prime * result + ((getUserPayId() == null) ? 0 : getUserPayId().hashCode());
        result = prime * result + ((getuId() == null) ? 0 : getuId().hashCode());
        result = prime * result + ((getPay() == null) ? 0 : getPay().hashCode());
        result = prime * result + ((getPayTime() == null) ? 0 : getPayTime().hashCode());
        return result;
    }
}